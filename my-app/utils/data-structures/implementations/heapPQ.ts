import { PriorityQueue, ListNode } from "../interfaces/priorityQueue";

/**
 * Implementation of a Priority Queue using a List
 * peek: O(1)
 * pop: O(logn)
 * insert: O(logn)
 */

class heapPQ<T> implements PriorityQueue<ListNode<T>> {}
